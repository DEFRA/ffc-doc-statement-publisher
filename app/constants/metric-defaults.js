const DEFAULT_TOTAL_STATEMENTS = 0
const DEFAULT_PRINT_POST_COST = 0
const DEFAULT_PRINT_POST_UNIT_COST = 200
const DEFAULT_COUNT = 0

const METRIC_ID_BIGINT = 'BIGINT'
const METRIC_DATE_ONLY = 'DATEONLY'
const METRIC_DATE = 'DATE'
const METRIC_STRING = 'STRING'
const METRIC_INTEGER = 'INTEGER'

const PERIOD_TYPE_MAX_LENGTH = 15
const SCHEME_NAME_MAX_LENGTH = 50

module.exports = {
  DEFAULT_TOTAL_STATEMENTS,
  DEFAULT_PRINT_POST_COST,
  DEFAULT_PRINT_POST_UNIT_COST,
  DEFAULT_COUNT,
  METRIC_ID_BIGINT,
  METRIC_DATE_ONLY,
  METRIC_DATE,
  METRIC_STRING,
  METRIC_INTEGER,
  PERIOD_TYPE_MAX_LENGTH,
  SCHEME_NAME_MAX_LENGTH
}
