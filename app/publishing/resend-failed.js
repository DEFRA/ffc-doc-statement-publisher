const resendFailed = async () => {
    // TODO get relevant failures out of failures table and send publish statement 
    // message to resend as a letter
    return true
}

module.exports = resendFailed